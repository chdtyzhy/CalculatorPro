# CalculatorPro UI 设计规范

## 设计定位
- 风格：轻量、离线的计算工作台。
- 必须形成独立视觉语言，不复刻系统计算器的黑底、圆形灰色键和橙色运算键组合。
- 最低支持 iOS 16.0。iOS 26 及以上交互控件使用系统原生 Liquid Glass API；iOS 16–25 使用同色系圆角实体材质与按压反馈。

## 屏幕尺寸基准
- 设计基准: iPhone 14 Pro (393 x 852 points)
- 安全区域: top 47pt, bottom 34pt

## 布局结构

### 1. 显示区域 (Display Area)
- **位置**: 顶部
- **结构**：顶部工具栏 + 深色结果显示屏。
- **背景**：雾白工作台背景上的深蓝灰显示屏。
- **圆角**：显示屏 8pt，保持工具属性而非卡片堆叠。

#### 显示内容
- **输入过程中**: 完整算式作为主显示（如 "123+45"），不显示占位结果
- **计算完成后上一行**: 完整算式（如 "123+45"）
  - 字体: SF Pro Regular, 24pt
  - 颜色: 灰色 (#8E8E93)
  - 对齐: 右对齐
- **主显示**: 输入中的完整算式，或计算完成后的结果
  - 字体: SF Pro Light, 64pt
  - 颜色: 白色 (#FFFFFF)
  - 对齐: 右对齐
  - 最小缩放: 0.3

### 2. 按钮区域 (Button Area)
- **位置**: 显示区域下方
- **高度**: 屏幕剩余高度 - 安全区域
- **背景**: 雾白 (#F0F7F5)

#### 按钮布局
- **列数**: 4 列
- **行数**: 5 行
- **按钮间距**: 12pt (水平和垂直)
- **边距**: 左右 16pt, 底部 20pt

#### 按钮尺寸计算
```
可用宽度 = 屏幕宽度 - 左右边距(32pt)
按钮宽度 = (可用宽度 - 间距(36pt)) / 4
按钮高度 = 按钮宽度 (正方形)
```

以 iPhone 14 Pro (393pt) 为例:
- 可用宽度 = 393 - 32 = 361pt
- 按钮宽度 = (361 - 36) / 4 = 81.25pt ≈ 81pt

#### 按钮样式

**数字按钮 (0-9, .)**
- 材质: 原生 Liquid Glass regular，半透明白色语义 tint
- 文字: 深蓝灰, 30pt, Semibold
- 形状: 16pt 小圆角方形

**功能按钮 (C, +/-, %, ⌫)**
- 材质: 原生 Liquid Glass regular，柔和杏色语义 tint
- 文字: 深蓝灰, 30pt, Semibold
- 形状: 16pt 小圆角方形

**运算符按钮 (+, −, ×, ÷)**
- 材质: 原生 Liquid Glass regular，低饱和长春花蓝语义 tint
- 文字: 白色, 30pt, Semibold
- 形状: 16pt 小圆角方形

**等号按钮 (=)**
- 材质: 原生 Liquid Glass regular，青绿色语义 tint
- 文字: 白色, 30pt, Semibold
- 形状: 16pt 小圆角方形

**顶部工具按钮**
- 位置: 顶部工具栏右侧
- 样式: 系统 `.glass` ButtonStyle
- 关于按钮图标: info.circle；点击后打开包含本地隐私说明、隐私政策、技术支持和问题反馈的 Sheet
- 历史按钮图标: clock.arrow.circlepath；点击后从底部打开历史 Sheet
- 顶部工具栏使用中文“计算器”标题，高度 36pt，不在安全区下方增加额外顶部留白

#### 特殊按钮
- **0 按钮**: 占 2 列宽度 (2 × 按钮宽度 + 间距)

### 3. 按钮排列

```
第1行: [C] [+/-] [%] [⌫] [历史]
第2行: [7] [8] [9] [÷]
第3行: [4] [5] [6] [×]
第4行: [1] [2] [3] [−]
第5行: [0    ] [.] [+] [=]
```

## 颜色规范

| 元素 | 颜色 | Hex |
|------|------|-----|
| 页面背景 | 雾白 | #F0F7F5 |
| 显示屏 | 深蓝灰 | #213547 |
| 数字按钮 | 半透明白 | white 72% |
| 功能按钮 | 柔和杏色 | #EDC29C |
| 运算符 | 长春花蓝 | #6478A6 |
| 等号 | 青绿色 | #33827A |
| 主文字 | 白色 | #FFFFFF |
| 次要文字 | 灰色 | #8E8E93 |

## 字体规范

| 用途 | 字体 | 大小 | 字重 |
|------|------|------|------|
| 显示数字 | SF Pro | 64pt | Light |
| 操作预览 | SF Pro | 24pt | Regular |
| 数字按钮 | SF Pro | 32pt | Medium |
| 功能按钮 | SF Pro | 28pt | Medium |
| 运算符 | SF Pro | 36pt | Medium |

## 历史记录

- 等号和百分比完成计算后自动保存算式、结果和时间。
- 记录仅保存在本机，最多保留 100 条。
- 点击记录将结果带回主界面，作为下一次计算的起点。
- 每条记录可从房租、餐饮、购物、交通、水电、工资中快速选择类别，也支持最多 20 个字符的自定义类别。
- 点击类别标签打开半屏选择面板；预设类别采用自适应两列图标网格，选择后立即保存。
- 自定义类别在同一面板内输入，已有类别可直接移除，不叠加第二层弹框。
- 历史记录在窄屏上将类别与算式放在首行、结果独占次行，动态文字不得互相遮挡。
- 长按记录可复制完整算式与结果；左滑可删除，工具栏可清空。

## 交互规范

- 按钮点击反馈: Liquid Glass `interactive()` 提供系统缩放、回弹和光泽反馈
- 键盘玻璃元素统一放入 `GlassEffectContainer`，保证渲染和相邻形状交互一致
- 历史记录: 从底部弹出 Sheet

## 适配规则

1. **小屏幕 (iPhone SE)**: 减小按钮间距至 8pt, 字体相应缩小
2. **大屏幕 (iPhone Pro Max)**: 保持按钮大小, 增加间距
3. **iPad**: 居中显示, 最大宽度 400pt
